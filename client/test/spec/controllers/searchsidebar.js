'use strict';

describe('Controller: SearchSidebarCtrl', function () {

  // load the controller's module
  beforeEach(module('embeditor'));

  var ctrl, scope;
/*
  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    ctrl = $controller('SearchSidebarCtrl', {$scope: scope});

    sidebar = angular.element('<embd-search-sidebar></embd-search-sidebar>');
    $compile(sidebar)(scope);
    scope.$digest();
  
  }));

  it('should toggle the sidebar when youTubeData executes a string query', function () {
   spyOn(ctrl.mdSidenav('searchSidebar'), 'toggle'); 
   ctrl.youTube.query('bbc open university');
   expect( ctrl.mdSidenav('searchSidebar').toggle ).toHaveBeenCalled();
  });

  it('should close the sidebar if a search item is clicked to play', function(){
    spyOn(ctrl.mdSidenav('searchSidebar'), 'close');

  });
*/
});
